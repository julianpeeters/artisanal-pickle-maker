package avocet 

import models._
import java.util.Arrays
import scala.reflect.internal.pickling._

import org.specs2._
import mutable._
import specification._

class OptionOptionOptionOptionSpec extends mutable.Specification {
/*
  val mySig = new ScalaSig(List("case class"), List("models", "MyRecord_OptionOption"), List(("p1", "Option"), ("p2", "Option")))
  val correctSig: String = {
    val scalaSigAnnot = classOf[MyRecord_OptionOption].getAnnotation(classOf[scala.reflect.ScalaSignature])
    val encodedBytes  = scalaSigAnnot.bytes
    encodedBytes
  }

  "a ScalaSig for case class MyRecord_OptionOption(p1: Option[], p2: Option[])" should {
    "have the correct string" in {
      mySig.bytes === correctSig
    }
  }
*/
}
